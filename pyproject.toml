[project]
name = "marimo-playground"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.0",
]

[dependency-groups]
dev = [
    "marimo>=0.23.1",
]

[tool.fastapi]
entrypoint = "main:app"
